import { View, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

const LatePasses = ({ navigation }) => {
    return (
        <View>
            <Text>Admin LatePasses</Text>
        </View>
    )
}

export default LatePasses